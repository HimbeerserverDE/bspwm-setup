#! /bin/bash

bspc node -f $1
bspc node $1 -t fullscreen
bspc node $1 -l above

while [ -n "`bspc query -N -n $1`" ]; do
	while [[ "`bspc query -N -n focused`" == "$1" ]]; do sleep 1; done

	bspc node -f $1
	bspc node $1 -t fullscreen
	bspc node $1 -l above

	sleep 1

	killall -9 su
done
